# Control Knowledge Schema

`page_id` is the stable wiki-relative path without `.md`. Relations are typed arrays of exact `[[page_id]]` links. `evidence_refs` identify immutable snapshots, traces, incidents, or design records. `status` records the review lifecycle. Runbook, Decision, Policy, and governance pages are protected: agents may create proposals but a human must approve direct changes.
