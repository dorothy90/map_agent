# Multi-Agent Knowledge Instructions

Read `wiki/index.md` first and follow its local indexes. Treat executable code as runtime authority. Never place domain data, user data, prompts, queries, result rows, or artifact payloads in this bundle. Submit proposals for protected Runbook, Decision, Policy, and governance pages. Run `python ../control_knowledge_validator.py .` before editing or publishing pages.
